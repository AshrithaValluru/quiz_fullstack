
  # Interactive Quiz Builder

  This is a code bundle for Interactive Quiz Builder. The original project is available at https://www.figma.com/design/EToQTDxkToZyk5DVrruR2B/Interactive-Quiz-Builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  